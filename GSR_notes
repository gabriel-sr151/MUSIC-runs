DICAS GERAIS

> Ao longo do codigo Wmunu is the shear-stress tensor

> pra mudar a pasta na qual os arquivos de output serao adicionados, basta trocar a localizacao do codigo compilado
   mas dai eh necessario tambem trocar o caminho do EoS por exemplo 
   eh vantajoso criar varias copias do codigo compilado quando houver varios jobs sendo rodados. Neste caso se houver apenas um arquivo
   pra eos todos os jobs vao tentar acessa-lo ao mesmo tempo e eles vao se atropelar
[!!!!!!]

> quando rodar o MUSIC em 3D fazer o plateau de forma que haja uma parte vazia grande  ____|------|____ e nao _|------|_ 

> the discretization flags can be overruled by e.g. the initial state data discretization data

##################################################################################################################
SPECIFIC PARTS OF THE CODE

> src/init.cpp contem a sintaxe da condicao inicial.

   p. ex. na funcao Init::initial_IPGlasma_XY_with_pi tem na linha 598 'ss >> dummy1 >> dummy2 >> dummy3 >> utau >> ux >> uy >> ueta >> ...'  

> src/grid_info.cpp contem a informacao sobre os outputs  
 
  >> evolution output

  syntax to print bulk pressure in mode (line 238) fprintf(out_file_bulkpi_xyeta,"%e %e %e\n", bulk_Pi, enthropy, cs2_local);
  (line 120) out_name_bulkpi_xyeta = "temp-output/evolution_bulk_pressure_xyeta.dat"

  fprintf(out_file_xyeta, "%e %e %e %e %e\n",
                            T_local*hbarc, muB_local*hbarc, vx, vy, vz);  

  >> movie output
  
  --header output

    see lines 1280 to 1290

> src/read_in_parameters.cpp contem a informação sobre o valores padrão das entradas (para o fator do tau_BULK por exemplo)

> 'include_second_order_terms' flag and dissipative.cpp

   terms tau_pipi (shear-sigma), phi7 (shear-shear) excluded line 183 -- 185 controlled by Include_second_order_terms

   term phi6 (bulk-sigma)  line 348 -- 359 controlled by Include_second_order_terms
   
   term delta_pipi (shear-theta) is NOT controlled by Include_second_order_terms  see variable tempf line 

   term phi7 (bulk-bulk) controlled by Include_second_order_terms indirectly via include_BBterm

      if (DATA.include_second_order_terms == 1) {
           include_BBterm = 1;
           include_coupling_to_shear = 1;
      }

   terms Wsigma, WW, Shear_Sigma_term, Shear_Shear_term controlled by include_coupling_to_shear   
   
   term delta_pipi (shear-theta) is NOT controlled by Include_second_order_terms  see variable tempf

> 'output_evolution_data' flag

   this flag takes values 1 2 3 4 depending on how you want the output to be 
   see lines 130 to 137 of evolve.cpp




